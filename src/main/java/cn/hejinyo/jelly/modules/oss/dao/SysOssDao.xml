<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.hejinyo.jelly.modules.oss.dao.SysOssDao">

    <resultMap id="BaseResultMap" type="cn.hejinyo.jelly.modules.oss.model.SysOss">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="url" jdbcType="VARCHAR" property="url"/>
        <result column="create_date" jdbcType="TIMESTAMP" property="createDate"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, url, create_date
    </sql>

    <sql id="Base_Where_SQL">
        <where>
            <if test="null != url">and url = #{url}</if>
            <if test="null != createDate">and create_date = #{createDate}</if>
        </where>
    </sql>

    <select id="findOne" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_oss
        WHERE id = #{value}
    </select>

    <select id="findPage" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from sys_oss
        <where>
            <if test="null != url">and url like concat('%',#{url},'%')</if>
            <if test="null != createDate">and create_date like concat('%',#{createDate},'%')</if>
        </where>
    </select>

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_oss (`url`, `create_date`)
        VALUES (#{url}, #{createDate})
    </insert>

    <update id="update">
        update sys_oss
        <set>
            <if test="url != null">`url` = #{url},</if>
            <if test="createDate != null">`create_date` = #{createDate}</if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM sys_oss
        WHERE id = #{value}
    </delete>

    <delete id="deleteBatch">
        delete from sys_oss where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>